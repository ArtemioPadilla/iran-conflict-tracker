---
import type { KpiItem } from '../../lib/schemas';

interface Props {
  kpis: KpiItem[];
}

const { kpis } = Astro.props;
---
<section class="kpi-strip fade-in">
  <div class="kpi-grid">
    {kpis.map(k => (
      <div class="kpi-card">
        <div class="kpi-label">{k.label}</div>
        <div class={`kpi-value ${k.color}`}>{k.value}</div>
        <div class="kpi-source">
          {k.source}
          {k.contested && <span class="kpi-contested">{k.contestNote || 'CONTESTED'}</span>}
        </div>
      </div>
    ))}
  </div>
</section>
